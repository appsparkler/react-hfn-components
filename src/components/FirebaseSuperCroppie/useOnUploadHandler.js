export default ({
  onUpload, storageRef,
}) => {
  return {
    onUploadHandler: () => {
      /*
        TODO set the on-upload-handler here
        Similar to : ~/src/legacy/FirebaseFileInput/CompositeFileInput
          /FileInput/useFileInput.js
        on line # 15 (handleDone method)
      */
      storageRef
      onUpload
    },
  }
}
